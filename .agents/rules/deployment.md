# Git & Deployment Rule

- **Always Commit and Push**: After completing any set of code edits or fixes and verifying that `npm run build` succeeds, always stage all changes (`git add -A`), commit with a descriptive message, and push to `origin main` (`git push origin main`) so that Vercel deploys the updates automatically.
